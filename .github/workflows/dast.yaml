name: DAST - OWASP ZAP

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    - cron: '0 1 * * 0' # Weekly on Sundays at 01:00

jobs:
  dast-zap:
    name: DAST - ZAP Active Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Start application
        run: |
          npm start > app.log 2>&1 &
          echo $! > app.pid

      - name: Wait for application to be ready
        run: |
          for i in {1..30}; do
            if curl -sSf http://127.0.0.1:3000/ > /dev/null; then
              echo "Application is up"
              exit 0
            fi
            sleep 2
          done
          echo "Application did not start in time"
          cat app.log || true
          exit 1

      - name: Prepare report directory
        run: |
          mkdir -p .zap-reports
          chmod 777 .zap-reports

      - name: Run OWASP ZAP Active Scan (XSS Focus)
        run: |
          docker run --rm --network host -u root \
            -v "${{ github.workspace }}/.zap-reports:/zap/wrk/:rw" \
            ghcr.io/zaproxy/zaproxy:stable \
            zap-full-scan.py \
            -t "http://127.0.0.1:3000/vuln/xss?q=test" \
            -J zap-active-report.json \
            -r zap-active-report.html \
            -x zap-active-report.xml \
            -m 3 || true

      - name: Upload ZAP report artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: |
            .zap-reports/zap-active-report.html
            .zap-reports/zap-active-report.json
            .zap-reports/zap-active-report.xml
            app.log

      - name: Stop application
        if: always()
        run: |
          if [ -f app.pid ]; then
            kill "$(cat app.pid)" || true
          fi